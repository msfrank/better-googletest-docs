[project]
name = "better-googletest-docs"
version = "0.1.0"
description = "Googletest documentation with better usability"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "myst-parser>=5.0.0",
    "sphinx>=9.1.0",
]
